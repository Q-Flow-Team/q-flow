# Base URL of the Q-Flow backend API (must include the /api/v1 suffix).
NUXT_PUBLIC_API_BASE=https://q-flow-backend.vercel.app/api/v1

# Set to true only for the frontend-only demo mode (in-browser mock API).
NUXT_PUBLIC_USE_MOCK=false
